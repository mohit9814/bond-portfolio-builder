export interface CompanyOverride { action: 'INCLUDE' | 'EXCLUDE'; justification: string; timestamp: number; }
export function getCompanyOverrides(): Record<string, CompanyOverride> { try { const data = localStorage.getItem('bond-company-overrides'); return data ? JSON.parse(data) : {}; } catch (e) { return {}; } }
export function setCompanyOverride(issuer: string, action: 'INCLUDE' | 'EXCLUDE', justification: string) { const overrides = getCompanyOverrides(); overrides[issuer.trim().toUpperCase()] = { action, justification, timestamp: Date.now() }; localStorage.setItem('bond-company-overrides', JSON.stringify(overrides)); window.dispatchEvent(new Event('portfolio-overrides-changed')); }
export function removeCompanyOverride(issuer: string) { const overrides = getCompanyOverrides(); delete overrides[issuer.trim().toUpperCase()]; localStorage.setItem('bond-company-overrides', JSON.stringify(overrides)); window.dispatchEvent(new Event('portfolio-overrides-changed')); }
